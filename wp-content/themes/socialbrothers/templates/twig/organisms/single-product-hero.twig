<div class="section bg-supporting-5 pt-[84px] pb-10">
    <div class="container">
        <div class="w-full flex relative">
            {% if safety_index %}
               	<img src="{{safety_index|raw}}" alt="safety index" class="w-[52px] h-[52px] object-contain absolute top-0 left-0">
            {% endif %}

            <div class="w-full lg:w-5/12 lg:ml-1/12">
                {% if product_image %}
                    {% include "atoms/image.twig" with {iamge_id: product_image} %}
                {% endif %}      
            </div>

            <div class="w-full lg:w-5/12">
            
               {% include "atoms/heading/h3.twig" with {title} %}
            
            
            </div>
        </div>
    </div>
</div>